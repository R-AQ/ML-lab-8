# ML-lab-8

This lab uses KNN on the dataset. The data was scaled using StandardScaler, then split into training and testing sets. A KNN model was trained and evaluated using a confusion matrix and classification report. Different K values were tested to find the best one, then the model was retrained for better results.
